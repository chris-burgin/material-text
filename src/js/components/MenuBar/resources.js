// ****************
// Instructions
// ****************
// Use this file to import resources from /src/js/resources
// and expose them to the component locally. Allows shared resources
// but keeps the component together still. Allows the benifit for having
// all external references in one location.

// import all require resources
import trash from '../../resources/svg/trash.jsx'

// re-export those resources
export {
  trash
}
