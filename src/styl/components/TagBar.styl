.tagbar {
  height: 2.4rem
  border-top: 1px solid rgba(0,0,0,.12);
  border-bottom: 1px solid rgba(0,0,0,.12);
  background-color: white;
  position: absolute;
  margin-top: 4rem;
  width: 100%;
  z-index: 10;
}
